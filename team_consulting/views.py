from django.shortcuts import render,HttpResponse

# Create your views here.
def first(request):
    return HttpResponse("In proccess.. Were going to add a consulting app for clients to put up meeting with our consultants")